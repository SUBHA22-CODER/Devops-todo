# DevOps Todo ✅

A sleek, animated Todo Web App designed with a **clean DevOps aesthetic**.  
This application uses **localStorage** for task storage and is **containerized with Docker** and served using **Nginx**.

---

## 🌟 Features

- Add, Complete, and Delete tasks
- Tasks persist using browser localStorage
- Smooth glowing hover animations
- Falling leaf background for a calm UI experience 🍃
- Responsive and minimal UI
- Fully Dockerized → Ready for deployment

---

## 🖥️ Tech Stack

| Layer     | Technology |
|----------|------------|
| UI       | HTML, CSS, JavaScript |
| Storage  | Browser localStorage |
| Server   | Nginx |
| Runtime  | Docker Container |

---

## 🐳 Docker Setup

Build the Docker image:

```bash
docker build -t devops-todo .
# Devops-todo
A modern and minimal DevOps-themed Todo Web App built with HTML, CSS, and JavaScript — fully containerized using Docker and served via Nginx.  Features include task add/delete/complete, localStorage persistence, glassmorphism UI, smooth hover animations, and calming falling leaf background effects.
